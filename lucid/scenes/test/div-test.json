{
  "title": "Div Expression Test",
  "subtitle": "Minimal test: does div expression work?",
  "version": "1.1",
  "notes": "Two spheres: left uses direct var, right uses div. Both should scale with 'length' slider. Check GLSL tab for 'uniform float u_length' and '(u_length / 2.0)'.",
  "params": {
    "length": { "value": 2.0, "type": "scalar", "min": 0.5, "max": 4.0 }
  },
  "root": {
    "type": "union",
    "children": [
      {
        "comment": "LEFT sphere - uses length DIRECTLY (should work)",
        "type": "sphere",
        "params": {
          "r": { "var": "length" },
          "color": [0.2, 0.8, 0.2]
        },
        "transform": { "translate": [-3, 0, 0] }
      },
      {
        "comment": "RIGHT sphere - uses length in DIV expression",
        "type": "sphere",
        "params": {
          "r": { "expr": "div", "args": [{ "var": "length" }, 2] },
          "color": [0.8, 0.2, 0.2]
        },
        "transform": { "translate": [3, 0, 0] }
      }
    ]
  }
}
