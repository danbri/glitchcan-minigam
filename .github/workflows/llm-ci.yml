name: LLM checks & auto-exec

on:
  push:
    paths:
      - '**/*.fink.js'
      - 'reports/autoexec.bot/*-input.txt'
  pull_request:
    paths:
      - '**/*.fink.js'
      - 'reports/autoexec.bot/*-input.txt'

jobs:
  llm:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout repository            # 1
        uses: actions/checkout@v4

      - name: Cache llama.cpp & model        # 2
        uses: actions/cache@v4
        with:
          key: qwen15-code-7b-v2-${{ runner.os }}
          path: |
            ~/.cache/llama
            ~/llama.cpp

      - name: Build llama.cpp                # 3
        if: steps.cache.outputs.cache-hit != 'true'
        run: |
          sudo apt-get update -y
          sudo apt-get install -y build-essential cmake libcurl4-openssl-dev
          git clone --depth 1 https://github.com/ggerganov/llama.cpp ~/llama.cpp
          cmake -S ~/llama.cpp -B ~/llama.cpp/build -DLLAMA_BUILD_EXAMPLES=OFF
          cmake --build ~/llama.cpp/build --config Release -j$(nproc)

      - name: Fetch model                    # 4
        run: |
          set -euo pipefail
          MODEL_DIR="$HOME/.cache/llama"
          FILE="$MODEL_DIR/codeqwen-1_5-7b-chat-q4_0.gguf"
          URL="https://huggingface.co/Qwen/CodeQwen1.5-7B-Chat-GGUF/resolve/main/codeqwen-1_5-7b-chat-q4_0.gguf"
          mkdir -p "$MODEL_DIR"
          [ -s "$FILE" ] || rm -f "$FILE"
          if [ ! -f "$FILE" ]; then
            echo "Downloading CodeQwen-1.5-7B-Chat Q4_0 (≈4.5 GB)…"
            curl -L --fail --retry 3 -o "$FILE" "$URL"
          fi

      - name: Install Python deps            # 5
        run: |
          pip install --upgrade pip
          pip install -q "ctransformers==0.2.27"

      - name: Patch llm_check.py             # 5a
        if: ${{ exists('tools/llm_check.py') }}
        run: |
          sed -i 's|Path(os.environ\["MODEL_PATH"\])|os.environ["MODEL_PATH"]|g' tools/llm_check.py || true

      - name: Lint / Test with LLM           # 6
        env:
          MODEL_PATH: /home/runner/.cache/llama/codeqwen-1_5-7b-chat-q4_0.gguf
        run: python tools/llm_check.py

      - name: Auto-exec bot                  # 7
        env:
          MODEL: /home/runner/.cache/llama/codeqwen-1_5-7b-chat-q4_0.gguf
        run: |
          set -euo pipefail
          BIN="$HOME/llama.cpp/build/bin/llama"
          SYS="<|im_start|>system You are a concise assistant that replies in English.<|im_end|>"
          shopt -s nullglob
          for f in reports/autoexec.bot/*-input.txt; do
            [ -f "$f" ] || continue
            USER="<|im_start|>user
            $(cat "$f")<|im_end|>"
            PROMPT="$SYS
            $USER
            <|im_start|>assistant"
            OUT="${f/-input.txt/-output.txt}"
            "$BIN" -m "$MODEL" -p "$PROMPT" -n 256 --temp 0.7 > "$OUT"
            echo "Generated $OUT"
          done

      - name: Upload auto-exec outputs       # 8
        uses: actions/upload-artifact@v4
        with:
          name: autoexec-reports
          path: reports/autoexec.bot/*-output.txt