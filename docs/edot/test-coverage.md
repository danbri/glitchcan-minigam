# edot — Unit Test Coverage (generated)

_Generated by `magpie/edot/tools/test-inventory.mjs` from the test sources — re-run after adding tests._

**708 assertions across 36 suites.** Each line is a behavior the suite enforces.

| Area | Suites | Assertions |
|------|-------:|-----------:|
| auth | 1 | 31 |
| automations | 1 | 10 |
| backup | 1 | 27 |
| calendar | 2 | 35 |
| data | 2 | 49 |
| editor | 4 | 185 |
| feeds | 2 | 25 |
| groups | 4 | 37 |
| kernel/data | 3 | 49 |
| mail | 1 | 51 |
| maps | 4 | 70 |
| places | 3 | 30 |
| projects | 2 | 15 |
| shell | 2 | 17 |
| slides | 3 | 76 |
| tools | 1 | 1 |

---

## auth

### `magpie/edot/auth/test-auth.mjs` — 31 assertions

- login.html boots + exposes window.__auth
- PKCE S256 matches RFC 7636 vector
- code_verifier charset + length valid (43..128)
- authorize url: response_type=code
- authorize url: client_id present
- authorize url: redirect_uri present
- authorize url: scope includes openid
- authorize url: state present
- authorize url: nonce present
- authorize url: code_challenge present
- authorize url: code_challenge_method=S256
- discovery populates authorize endpoint
- discovery populates token endpoint
- discovery populates jwks endpoint
- token exchange returns an id_token
- decodeIdToken extracts claims (name/email)
- decodeIdToken rejects an invalid nonce
- decodeIdToken rejects a wrong aud
- handleRedirectCallback rejects mismatched state (CSRF)
- two identities coexist
- newly added account becomes active
- account switcher changes the active identity
- signOut removes exactly one account
- session persists to localStorage and reloads
- BroadcastChannel notifies sibling sessions
- full callback exchange creates a session account
- completed account lands in the shared session
- ships >=16 provider presets
- all required providers present
- GitHub flagged as non-OIDC (no id_token)
- no page errors

## automations

### `magpie/edot/automations/test-automations.mjs` — 10 assertions

- script result uses the event payload (1 + 5)
- edot.log reaches the host
- edot.invoke round-trips to a kernel capability
- sandbox has NO DOM (document & window undefined, self is the worker)
- sandbox has NO network (fetch/XHR/importScripts/WebSocket/Worker all undefined)
- a runaway script is killed by the timeout
- seed automations are present
- an automation drives slides.addData through the kernel
- the run log shows the automation output
- no page errors

## backup

### `magpie/edot/backup/test-backup.mjs` — 27 assertions

- page + __backup hook ready
- header shows the alpha sign-in chip
- crypto: encrypt→decrypt recovers exact bytes
- crypto: wrong passphrase fails
- crypto: flipped ciphertext byte fails (GCM)
- crypto: recorded SHA-256 matches plaintext
- crypto: PBKDF2 iterations >= 200k
- crypto: envelope is versioned (v=1)
- snapshot: manifest counts the seeded records
- snapshot: wipe empties the store
- snapshot: restore brings data back
- snapshot: binary (Uint8Array) value round-trips
- ${name}: put→get returns identical bytes
- ${name}: list returns the put id
- ${name}: remove works
- github: issues a Contents PUT
- webdav: list issues a PROPFIND
- solid: list issues a container GET
- e2e: snapshot→encrypt→put→get→decrypt recovers bytes
- e2e: restored snapshot fingerprint matches
- ui: ALPHA badge present
- ui: experimental warning present
- ui: renders the Back up control
- ui: backup via component succeeds
- ui: SHA-256 fingerprint shown after backup
- ui: snapshot appears in the list
- no page errors

## calendar

### `magpie/edot/calendar/test-calendar-share.mjs` — 7 assertions

- subscribing adds a calendar layer
- the layer is populated with the .ics events
- browse picker lists catalogue calendars
- Share to group invokes groups.share with the calendar
- shared payload carries the events
- Open as table invokes data.addTable with event rows
- no page errors

### `magpie/edot/calendar/test-calendar.mjs` — 28 assertions

- calendar app booted with a default layer
- ICS unescapes text values (comma/semicolon/newline)
- ICS parses a VALARM with TRIGGER
- ICS serialize re-escapes special chars
- ICS parse->serialize->parse round-trips event + alarm
- RRULE weekly COUNT=5 expands to 5 occurrences
- RRULE occurrences land on the same weekday
- RRULE occurrences are one week apart
- RRULE EXDATE excludes one occurrence (4 -> 3)
- event persists to IndexedDB and reloads
- Month view renders an ARIA grid
- Week view renders 7 day columns
- Day view renders 1 day column
- Agenda view renders a list
- persisted event renders as an occurrence
- hidden layer hides its events
- importing an .ics invite adds the event
- invite import keeps ORGANIZER + ATTENDEE
- due reminder detected in-app (fire-time passed)
- alarm code degrades gracefully without Notification permission
- calendar exports to a valid VCALENDAR
- search filters events by text
- no page errors
- mobile: ☰ toggle is shown
- mobile: ☰ opens the calendars drawer
- mobile: tapping the scrim closes the drawer
- mobile: drawer reopens and recloses
- mobile: no page errors

## data

### `magpie/edot/data/test-data.mjs` — 37 assertions

- engine (sqlite wasm) booted
- opens to the welcome start panel, not SQL
- header shows the alpha sign-in chip
- CSV imported as a table (sidebar)
- table grid renders rows
- amount is numeric in store
- grid edit -> UPDATE persists
- view created and queryable
- view appears in sidebar
- sheet formula =SUM(range) computes
- sheet =SQL() reads the database
- sheet -> SQL table (materialised, queryable)
- Chinook core tables loaded
- Chinook has ~3500 tracks
- Chinook 4-table join runs
- Chinook sidebar shows the tables
- sendToEditor writes an HTML-table handoff
- mobile drawer toggles via ☰
- N-Quads export emits typed quads
- N-Quads round-trips a table (name/cols/rows)
- N-Quads preserves numeric typing
- N-Quads export is deterministic (byte-identical)
- CSV export is deterministic (byte-identical)
- zip-of-CSVs round-trips entry + content
- export fingerprints content (sha256)
- grid sorts ascending on header click
- grid sorts descending on second click
- edit after sort writes back to the correct row
- datasheet face renders a grid with a 3-way switcher
- spreadsheet face renders a sheet
- RDF face shows the N-Quads form of the object
- new objects default into a folder (General)
- an object can be moved to another folder
- a project folder collects newly created objects
- no object is loose outside a folder
- database exports to bytes
- no page errors

### `magpie/edot/data/test-geo-formula.mjs` — 12 assertions

- QID("Bristol") -> Q23154
- WIKIDATA by name is the same id
- lookup works by QID too
- LAT("London") ~ 51.5
- LON("London") ~ -0.13
- GEOCODE returns "lat, lon"
- PLACEKIND("London") -> city
- GEODISTANCE London<->Edinburgh ~ 534 km
- GEO functions compose (ROUND of a distance)
- cell-ref arg works via a fake ctx.cell
- unknown place -> #N/A, catchable by IFERROR
- no gazetteer loaded -> #NAME?

## editor

### `magpie/edot/test-e2e.mjs` — 79 assertions

- typed text shows word count
- bold button pressed
- bold uses a tag, not a styled span
- PDF download filename ends .pdf
- DOCX download filename ends .docx
- edoc download filename ends .edoc
- edoc envelope is a doc with body.html
- edoc carries a 64-hex content fingerprint
- edoc id matches the live document id
- edoc bytes are deterministic (sorted keys)
- reimported .edoc restores the document text
- new doc resets word count
- library lists >=2 docs
- reopened doc restores text
- reopened doc kept bold tag
- title field shows reopened name
- align-center button pressed
- editor block is centered
- find bar is visible
- find shows 1/2 count
- find bar closes on Escape
- source dialog opens
- HTML source keeps RDFa + link
- Markdown source keeps the link but drops RDFa
- Plain text strips markup and links
- Open dialog lists the Morton example under Examples
- Open dialog has a Research folder
- Research folder is collapsed initially
- Research folder expands to the deep-dive report
- research markdown loads into the editor
- GFM tables render as real tables
- example loaded with tables
- example title set
- close yields an empty document
- close titles it Untitled
- url dialog opens
- url dialog shows a validation error
- github dialog opens
- token auto-harvested from clipboard
- default path is folder-encapsulated
- default branch auto-detected from repo
- bare filename wrapped into a folder
- github preview renders a diff
- github diffstat shows counts
- github commit opens a PR link
- merge button appears after PR
- PR merges when conflict-free
- save location cached
- recents render in the dialog
- a cached location can be zapped
- connected identity shows @login
- File menu shows the attached account
- token saved on device
- disconnect clears the saved token
- disconnect resets the menu hint
- github rejects binary save-back
- data handoff inserts a table into the doc
- handoff is consumed (cleared)
- long-press shows a readable label bubble
- held control grows (holding class)
- label bubble stays on-screen
- grow clears on release
- Labels mode shows text under icons
- Labels preference persists
- palette opens on Mod+K
- palette lists commands
- palette filters to a match
- palette runs the command (new doc)
- palette closed after running
- palette closes on Escape
- registry owns every export format
- File-menu action flows through run() (audit fired)
- saved doc is recorded in the object index
- index search finds the doc by title
- index never stores the document body
- ${label} export is deterministic (byte-identical re-export)
- editor blocks use per-paragraph bidi (unicode-bidi: plaintext)
- an all-RTL document resolves the surface to rtl
- no page errors

### `magpie/edot/test-editor-component.mjs` — 16 assertions

- two editors, no duplicate element ids across instances
- each editor has its own toolbar and a unique page id
- content property get/set round-trips
- stats() reflects content
- readonly config disables editing on A only
- host menu setBlock(h1) applied to A
- host menu align(center) applied to A
- editor B was NOT affected by commands run on A
- edot-change event fired the host dirty dot
- data share routed to the focused editor (B)
- the unfocused editor (A) did not receive the share
- after focusing A, the next share routes to A
- editor inserts an image as a data URL (survives the sanitizer)
- editor downscales a large bitmap on import
- editor keeps an imported SVG as vector
- no page errors

### `magpie/edot/test-edot.mjs` — 75 assertions

- app boots
- toolbar has buttons
- roving tabindex: one tab stop
- editor is contenteditable textbox
- markdown -> html has h1
- markdown -> html has strong/em/code
- markdown -> html has list
- html -> markdown preserves heading
- html -> markdown preserves bold
- docx blob is non-trivial
- docx -> html keeps heading
- docx -> html keeps bold
- docx -> html keeps italic
- docx -> html keeps underline
- docx -> html keeps bullet list
- docx -> html keeps numbered list
- docx -> html keeps blockquote text
- zip round-trip (deflate+store)
- sanitizer removes scripts
- sanitizer keeps content
- word count reflects content
- pdf starts with %PDF-
- pdf ends with EOF
- pdf has xref + multiple pages
- pdf is substantial
- library persists docs across reload
- library reports a backend kind
- rdfa property survives sanitize
- rdfa sanitize still drops handlers
- html export declares vocab + prefixes
- sanitize keeps text-align only
- docx emits w:jc center+right+both
- docx import restores alignment
- pdf with alignment renders
- find locates all matches
- replace all replaces every match
- docx import renders a table
- docx import embeds the image as data-URL
- docx import keeps document order (text, table, image)
- docx export+import preserves a table
- markdown export emits a GFM table
- sanitizer keeps table + colspan
- sanitizer keeps data:image but drops handlers + data:text/html
- github blob -> raw.githubusercontent
- raw github passes through (no cors risk)
- gist -> gist raw
- gitlab blob -> raw
- bitbucket src -> raw
- plain cross-origin url flagged corsRisk
- invalid url throws
- filename from path keeps extension
- filename from content-type when no ext
- examples manifest present
- destroy()+reinstantiate does not leak DOM
- diff counts edits + additions
- diff collapses unchanged runs into gaps
- git base64 round-trips unicode (incl. astral)
- git PUT commits decoded content
- git PUT uses existing sha + new branch
- git creates branch ref off base
- git PR head=branch, base=main
- git authorizes with bearer token
- git returns the PR number
- exportText returns source-format markdown
- exportText blocks binary formats
- symbol PUA decodes to logic glyphs
- symbol-font byte range decodes ∀∃⊃
- non-symbol ASCII is left intact
- isSymbolFont matches Symbol/SymbolMT only
- docx Symbol-font run imports ∀ (not a square)
- attention swaps favicon while away
- attention nudges the title while away
- attention restores title + disarms on return (once)
- LibreOffice reports unconfigured
- no console errors

### `magpie/edot/test-mobile.mjs` — 15 assertions

- toolbar has its buttons
- toolbar is not crushed (>=36px tall)
- tap on page focuses the editor
- tap on margin focuses the editor
- typing after tap reaches the editor
- File menu opens on tap
- menu button reports expanded
- a Save-as item is visible
- menu item action fired (library dialog open)
- html overflow hidden
- body overflow hidden
- document overscroll is none
- scroll region contains overscroll
- body itself does not scroll
- no page errors

## feeds

### `magpie/edot/feeds/test-feed.mjs` — 14 assertions

- RSS: reads channel title + items
- RSS: item has title/link/date
- Atom: reads feed title + entries
- Atom: resolves rel=alternate link, falls back to first
- feedToTable yields Title/Date/Link rows
- catalogue: feeds for a place by QID (Bristol→bbc-bristol)
- catalogue: calendars for a place by name (Scotland)
- catalogue: exposes feeds + calendars
- fetchFeedTable returns parsed items
- fetchIcsTable returns parsed events
- data.addTable surfaces a feed as a Data table
- data.addTable surfaces an .ics as a Data table
- surfaced tables appear in the Data object browser
- no page errors

### `magpie/edot/feeds/test-ics.mjs` — 11 assertions

- reads the calendar name (X-WR-CALNAME)
- parses both VEVENTs
- unescapes text (comma in SUMMARY)
- unescapes newlines in DESCRIPTION
- parses timed DTSTART/DTEND to ISO UTC
- event is not all-day
- retains the RRULE verbatim
- parses VALUE=DATE as an all-day date
- unfolds continuation lines
- icsToTable yields Summary/Start/End/Location/URL columns
- icsToTable has one row per event

## groups

### `magpie/edot/groups/test-groups.mjs` — 10 assertions

- demo seeds channels
- a channel is active and titled
- demo-mode status is shown
- participants render for the active channel
- the sent message appears in the timeline
- the echo bot replies in the channel
- my own message is right-aligned (.me)
- switching channel changes the timeline
- groups.share posts a shared card into the channel
- no page errors

### `magpie/edot/groups/test-sasl.mjs` — 7 assertions

- PLAIN response matches the known base64
- SCRAM client-first message is correct
- SCRAM client-final has channel binding + full nonce
- SCRAM client proof matches the RFC 5802 vector
- SCRAM verifies the server signature (RFC 5802 vector)
- SCRAM rejects a wrong server signature
- SCRAM rejects a server nonce that does not extend the client nonce

### `magpie/edot/groups/test-xmpp-handshake.mjs` — 10 assertions

- opens the stream with RFC 7395 framing
- chooses SCRAM-SHA-1 and sends <auth>
- auth payload is the SCRAM client-first message
- SCRAM response carries the RFC 5802 client proof
- verifies server signature and reports authenticated
- restarts the stream after auth
- requests resource binding
- binds the full JID and becomes ready
- sends initial presence when ready
- reports SASL failure with a reason

### `magpie/edot/groups/test-xmpp-stanzas.mjs` — 10 assertions

- join uses MIX-PAM client-join with the channel
- join wraps a MIX-CORE join
- join subscribes to messages + participants nodes
- join carries the nick
- leave uses MIX-PAM client-leave + MIX-CORE leave
- create targets the MIX service and names the channel
- groupchat message has type=groupchat to the channel
- message body is XML-escaped
- message carries an optional structured payload
- participant request is a pubsub items query on the participants node

## kernel/data

### `magpie/edot/test-data-object.mjs` — 12 assertions

- canonicalize sorts keys deterministically
- canonicalize preserves array order
- fingerprint matches known SHA-256("hello world")
- fingerprint differs for different content
- deck type → .edeck + media type
- unknown type → json fallback
- makeId is unique across 100 calls
- content fingerprint ignores metadata/key-order (same body → same fp)
- content fingerprint changes when the body changes
- serialize round-trips the body
- serialize carries id/type/schemaVersion
- finalize stamps a 64-hex content fingerprint

### `magpie/edot/test-kernel.mjs` — 18 assertions

- bus delivers to a local subscriber
- bus does not deliver across types
- unsubscribe stops delivery
- wildcard subscriber sees every type
- cross-tab: sibling receives the message
- no echo: publisher delivers to itself exactly once
- crossTab:false does not cross the transport
- has() reports a provided capability
- list() includes meta
- local invoke calls the provider directly
- local invoke returns the provider result
- unprovide removes the capability
- invoke with no provider and no bus throws
- remote invoke runs the provider on the sibling tab
- remote invoke returns undefined (fire-and-forget)
- kernel.index works
- kernel.bus works
- kernel.capabilities works

### `magpie/edot/test-object-index.mjs` — 19 assertions

- upsert requires id and type
- upsert returns the stored record
- get retrieves by id
- has/size reflect inserts
- the body is not stored in the index
- governance seams default null
- partial upsert preserves title
- partial upsert preserves fingerprint
- partial upsert preserves createdAt
- partial upsert updates modifiedAt
- all() sorts newest first
- byType filters
- search matches title
- search matches type
- empty search returns all
- remove reports it removed
- remove of a missing id reports false
- a new index over the same store reloads records
- clear empties the index and persists

## mail

### `magpie/edot/mail/test-mail.mjs` — 51 assertions

- mail header shows the sign-in chip
- sign-in chip upgraded (custom element rendered)
- chip is flagged alpha
- MIME decodes RFC2047 encoded-word subject
- MIME picks the html alternative
- MIME keeps the plain alternative too
- MIME quoted-printable decodes (=C3=A9 → é)
- MIME base64 decodes
- MIME extracts the attachment with decoded content
- sanitizer removes <script>
- sanitizer strips onclick handler
- sanitizer blocks remote <img> by default
- sanitizer seat-belts links (new tab, no referrer)
- sanitizer neutralizes javascript: href
- sanitizer loads remote img when allowRemote
- gmail.listMailboxes normalizes roles
- gmail mailbox carries unread count
- gmail.listMessages normalizes summary + flags + cursor
- gmail.getMessage decodes base64url html body
- gmail.sendMessage POSTs messages/send with base64url raw
- graph.listMailboxes normalizes roles
- graph mailbox carries unread count
- graph.listMessages normalizes from + hasAttachments
- graph.getMessage returns html body
- graph.sendMessage POSTs /me/sendMail with message shape
- graph.setFlags PATCHes isRead
- jmap.listMailboxes normalizes roles (junk→spam etc.)
- jmap mailbox carries unread count
- jmap.listMessages normalizes from + flags via keywords
- jmap.getMessage resolves htmlBody via bodyValues
- jmap.setFlags uses keyword patch
- jmap.sendMessage issues EmailSubmission/set
- imap.isAvailable true only with proxyUrl
- imap talks the proxy protocol (login/list/search/fetch/store/send)
- imap normalizes mailboxes via proxy list
- imap parses raw RFC822 via MIME (from/subject/body)
- imap without proxyUrl is unavailable and refuses honestly
- registry creates the right adapter
- registry lists all four backends
- UI renders the mailbox tree
- UI renders the message list
- UI marks unread messages bold
- UI opens a message into the reading pane
- UI reading pane blocks the remote tracker image
- UI shows the load-remote-content affordance
- UI loads remote content on demand
- compose builds a draft with to/subject/html
- compose send calls adapter.sendMessage with the draft
- search filters the message list
- send with no account shows a sign-in hint, not a crash
- no page errors

## maps

### `magpie/edot/maps/test-events-layer.mjs` — 6 assertions

- eventsToFeatures keeps only located events, carries the QID
- loadEventFeatures reads the two located events back (un-located skipped)
- features carry the calendar QIDs
- drawEvents adds the events source + circle/halo/label layers
- the source is fed the event features
- no page errors

### `magpie/edot/maps/test-maps-xr.mjs` — 3 assertions

- XR button is hidden when the device has no WebXR
- XR button is shown when the device supports immersive-vr
- XR button is labelled for the supported mode (VR)

### `magpie/edot/maps/test-maps.mjs` — 37 assertions

- component booted and built its controls
- MapLibre map object exists (smoke)
- geocode parser drops invalid + keeps valid results
- geocode parser normalises lat/lon to numbers
- geocode parser normalises bbox to [W,S,E,N]
- geocode URL carries the query + format
- route fixture parses to a 3-point polyline
- route distance is read from the response
- route summary formats distance + duration
- route URL is OSRM-shaped (lng,lat;lng,lat + geojson)
- formatters: metres/km/seconds/minutes/hours
- savePlace persists to IndexedDB
- saved place renders in the sidebar list
- updatePlace writes back the edited note
- deletePlace removes it from the store
- saved places survive a page reload
- places export to GeoJSON Point features
- GeoJSON round-trips name/note/coords
- import tolerates a minimal foreign export
- import skips non-Point geometry
- importGeoJsonFile adds both points
- places-layer toggle flips aria-pressed
- places-layer toggle sets layer visibility=none when hidden
- hash encodes z/lat/lng
- hash decodes back to center+zoom
- hash round-trips an optional marker
- non-map hash decodes to null
- applyHash jumps the map to the shared view
- raster basemap builds a valid inline style with OSM tiles
- raster basemap carries attribution
- {s} subdomains expand to multiple tile URLs
- vector basemap resolves to a style URL
- a building-capable vector basemap is configured (3D buildings have a source)
- setBasemap switches the active basemap + select
- directions panel opens
- "lat,lng" input resolves to [lng,lat] without network
- no page errors

### `magpie/edot/maps/test-maps3d.mjs` — 24 assertions

- DEM source is raster-dem with the configured tiles + encoding
- terrain spec references the DEM source + exaggeration
- sky spec carries sky/horizon/fog colours for setSky
- DEM builder returns null when no tiles configured
- buildings layer is a fill-extrusion with height expression
- detect prefers an existing fill-extrusion source/source-layer
- detect falls back to a vector source + guessed building layer
- detect returns null for a raster-only style
- KML parses to a FeatureCollection
- KML yields point/line/polygon + 2 from MultiGeometry (5 features)
- KML Point keeps lng,lat and drops altitude
- KML LineString has 3 vertices
- KML Polygon ring is closed (first==last)
- KML MultiGeometry expands to separate features w/ ExtendedData
- malformed KML throws rather than mis-parsing
- bounds spans all coordinates [[W,S],[E,N]]
- bounds is null for an empty collection
- 3D toggle button starts off and flips aria-pressed on/off
- toggle3D updates the is3D state field
- Buildings toggle button is present and toggleable
- KML import control exists in the toolbar
- addKmlGeoJson records the overlay FeatureCollection
- enabling 3D adds the DEM source (live map)
- no page errors

## places

### `magpie/edot/places/test-place-input.mjs` — 7 assertions

- option shows the name and its Wikidata QID
- combobox reports expanded while open
- arrow key sets aria-activedescendant
- place-selected fired with name + QID
- selected place carries coordinates
- listbox collapses after selection
- no page errors

### `magpie/edot/places/test-places.mjs` — 10 assertions

- wikidata.search returns name + QID
- local.search finds the seed entry with coords + QID
- local seed parsed all 7 rows
- merge collapses same-QID hits into one
- merged Place has both QID and coordinates
- merge records contributing sources
- rank puts the exact-name match first
- facade returns Edinburgh as one fused Place
- fused Place carries QID AND coordinates from two sources
- planned sources (overture/foursquare) were not queried

### `magpie/edot/places/tools/test-derive-places.mjs` — 13 assertions

- clips to bbox and drops the un-named feature (4 of 6)
- Delta Tower (outside bbox) is excluded
- Overture names.primary -> name
- Overture categories.primary -> kind
- Overture sources[].wikidata -> wikidataId
- plain GeoJSON properties.name + .wikidata
- plain .category -> kind
- missing wikidata -> null (not invented)
- non-Point geometry falls back to a coordinate
- every place carries the source id
- duplicate (same QID) is de-duplicated
- coordinates are normalized numbers
- toNdjson emits one parseable Place per line

## projects

### `magpie/edot/projects/test-project.mjs` — 8 assertions

- zip starts with PK magic
- round-trips entry names + bytes
- [${t.id}] manifest name survives the round-trip
- [${t.id}] all ${fileNames.length} content files survive
- [${t.id}] every manifest-declared file is present
- [${t.id}] deck ${s.id} parses with slides
- system unzip lists the archive (standards-valid zip)
- system unzip integrity test passes (CRCs correct)

### `magpie/edot/projects/test-projects-shell.mjs` — 7 assertions

- Projects is reachable from the shell and shows templates
- template doc hydrated into the Editor pane
- template deck hydrated into the Slides pane
- template table hydrated into the Data pane
- snapshot produces a manifest with a document and a deck
- snapshot bundles the document and deck files
- no page errors

## shell

### `magpie/edot/test-shell.mjs` — 9 assertions

- single shell: app rail + menu bar present
- Editor shows File / Edit / Insert / View / Help
- Editor menu action works (View → Heading 1)
- Data View menu offers data views (query / spreadsheet / datasheet)
- Data menu action works (load sample DB populates objects)
- View → Open app switches the active app
- mobile: Workspace is reachable in the rail (core surface, not hidden)
- mobile: Workspace opens with sub-tabs and shows one pane at a time
- no page errors

### `magpie/edot/test-workspace.mjs` — 8 assertions

- three panes mount: data, slides, editor
- editor pane built its formatting toolbar
- data share lands in the editor pane as one titled table
- the SAME share also reached the slides pane
- data pane has geo functions (=QID -> Q84)
- data pane =GEODISTANCE computes (~534 km)
- editor.getContent() returns the inserted document
- no page errors

## slides

### `magpie/edot/slides/test-samples.mjs` — 13 assertions

- component booted with codecs
- manifest loads from the app base and lists samples
- ${file} imports without error
- ${file} has >= ${exp.minSlides} slides
- ${file} preserves text "${exp.needle}"
- ${file} preserves >= ${exp.minImages} images
- ${file} re-imports without error
- ${file} re-imports with slides
- ${file} round-trip preserves "${n}"
- multilingual JSON→PPTX→import keeps Arabic
- multilingual JSON→PPTX→import keeps Hebrew
- multilingual JSON→PPTX→import keeps CJK
- no page errors

### `magpie/edot/slides/test-slides-edit.mjs` — 16 assertions

- two shapes rendered
- clicking an element selects it (4 resize handles + move grip)
- the inspector shows for the selection (Arrange controls)
- dragging the body moves the element (x increased)
- dragging the SE handle resizes the element (width grew)
- “Bring to front” re-layers the element to the top (last in order)
- clicking bare canvas deselects (inspector hidden)
- presenter view shows notes, next-slide preview and a timer
- present mode has a visible exit button (touch-reachable)
- clicking the exit button leaves present mode
- large bitmap is downscaled to the 1600px cap
- downscale preserves aspect ratio (2:1)
- image element is sized (positioned within page)
- SVG is embedded as vector (not rasterized)
- an element can be rotated (orientability)
- no page errors

### `magpie/edot/slides/test-slides.mjs` — 47 assertions

- component booted with a deck
- deck has 2 slides after build
- title text persisted to IndexedDB
- bullet bold flag persisted
- speaker notes persisted
- saved deck is recorded in the shared object index
- contenteditable edit syncs into the deck core
- addSlide inserts a slide
- duplicateSlide adds a copy
- moveSlide reorders
- deleteSlide removes a slide
- JSON round-trip is lossless
- JSON export is idempotent
- PPTX contains presentation.xml
- PPTX contains slide1.xml
- PPTX contains [Content_Types].xml
- PPTX slide1.xml carries the title text
- PPTX content-types declares slide parts
- PPTX emits a notes slide for noted slides
- PPTX blob is application/zip
- PPTX round-trip recovers slide count
- PPTX round-trip recovers title text
- PPTX round-trip recovers body bullets
- PPTX round-trip recovers notes
- PPTX round-trip carries an image element
- PPTX round-trip image is a PNG data URL
- ODP round-trip recovers slide count
- ODP round-trip recovers title
- ODP round-trip recovers body
- ODP round-trip recovers notes
- present mode renders an overlay
- present next advances the index
- present prev rewinds the index
- Esc exits present mode (overlay removed)
- ArrowRight advances in present mode
- Escape key exits present mode
- PDF export starts with %PDF
- PDF export is non-empty
- PDF has one page per slide
- PDF blob is application/pdf
- HTML export contains all slide texts
- HTML export is self-contained (no remote refs)
- HTML export is keyboard-navigable
- HTML export has one section per slide
- PPTX export is deterministic (byte-identical)
- ODP export is deterministic (byte-identical)
- no page errors

## tools

### `magpie/edot/tools/test-inventory.mjs` — 1 assertions

- <assertion>

